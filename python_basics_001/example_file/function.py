def compute_list_average(number_list):
    return sum(number_list) / len(number_list)

number_list = [4, 7, 3, 12] # Using list data type for the local variable
print(compute_list_average(number_list))